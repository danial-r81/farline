import Select from './select/Select';

import './courses.css';
const Courses = () => {
   return (
      <section className='courses'>
         <Select />
      </section>
   );
};

export default Courses;
